create table LINE
(
	ORDER_ID INTEGER not null
		constraint LINE_ORDER_ORDER_ID_FK
			references "ORDER",
	PART_ID INTEGER not null
		constraint LINE_PART_PART_ID_FK
			references PART,
	LINE_QTY INTEGER,
	constraint LINE_PK
		primary key (ORDER_ID, PART_ID)
);


