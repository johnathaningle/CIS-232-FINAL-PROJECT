create table "ORDER"
(
	ORDER_ID INTEGER default AUTOINCREMENT: start 1 increment 1 generated always as identity
		constraint ORDER_PK
			primary key,
	CUSTOMER_ID INTEGER
		constraint ORDER_CUSTOMER_CUSTOMER_ID_FK
			references CUSTOMER
);

